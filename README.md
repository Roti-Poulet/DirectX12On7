# DirectX12On7
A patch to get DirectX12 running on Windows 7, using Microsoft DLLs

This is a real implementation of DirectX12, not just an emulation using Vulkan (VKD3D), so it's a bit more optimized.
